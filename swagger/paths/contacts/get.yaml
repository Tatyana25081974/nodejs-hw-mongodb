tags:
  - Contacts

summary: Get all contacts
operationId: getAllContacts
description: Returns a list of user contacts with filtering, sorting, and pagination.

security:
  - bearerAuth: []

parameters:
  - in: query
    name: type
    schema:
      type: string
      enum: [work, home, personal]
    description: Filter by contact type

  - in: query
    name: isFavourite
    schema:
      type: boolean
    description: Filter by favourite status (true/false)

  - in: query
    name: page
    schema:
      type: integer
      default: 1
    description: Page number for pagination

  - in: query
    name: perPage
    schema:
      type: integer
      default: 10
    description: Number of contacts per page

  - in: query
    name: sortBy
    schema:
      type: string
      enum: [name, email, contactType]
    description: Field to sort by

  - in: query
    name: sortOrder
    schema:
      type: string
      enum: [asc, desc]
    description: Sort direction (ascending or descending)

responses:
  '200':
    description: A list of contacts
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
            - page
            - perPage
            - totalItems
            - totalPages
            - hasPreviousPage
            - hasNextPage
          properties:
            status:
              type: integer
              example: 200
            message:
              type: string
              example: Successfully fetched contacts!
            data:
              type: array
              items:
                $ref: '../../components/schemas/contact.yaml'
            page:
              type: integer
              example: 1
            perPage:
              type: integer
              example: 10
            totalItems:
              type: integer
              example: 42
            totalPages:
              type: integer
              example: 5
            hasPreviousPage:
              type: boolean
              example: false
            hasNextPage:
              type: boolean
              example: true

  '401':
    $ref: '../../components/responses/401.yaml'

  '500':
    $ref: '../../components/responses/500.yaml'
